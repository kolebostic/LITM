using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VoicelineReader : MonoBehaviour
{
    private string scene;
    // Start is called before the first frame update
    void Start()
    {
        scene = "scene";
        print(scene);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
